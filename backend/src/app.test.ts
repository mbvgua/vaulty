import app from "./app";

describe("[app setup]", ()=>{

    test("the server running", async()=>{
        expect(app).toBeDefined()
    })

    // database connection is successful

    // unkown routes get 404 error

    // parse-json in request body
})
