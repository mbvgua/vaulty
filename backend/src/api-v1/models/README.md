# Models

This directory helps in statically defining the various variables within the application, in turn adding type support to it. To get a better understanding of the files therein, they will be discussed in terms of the modules that they represent:

---

1. Admin -> 

---
2. Users -> Handled in the `user.model.ts` file. Deals with all users data types:
    - **UserRoles**: strong type all the user roles within application.
    - **Users**: define how the users object will be shaped for easier integration with the database.

---
